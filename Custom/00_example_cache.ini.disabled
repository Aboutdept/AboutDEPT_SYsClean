; ============================================================
;  SysClean 自定义清理规则 —— 示例（默认禁用）
; ============================================================
;  本文件以 .disabled 结尾，SysClean 不会加载它。
;  若要启用：把文件名改为  example_cache.ini  （去掉 .disabled）。
;
;  语法（winapp2 子集）：
;    [规则显示名]             <- 方括号内是该条规则的显示名称
;    DetectFile=路径           <- 安装检测：该路径存在才视为「已安装」（支持 %VAR%）
;    Detect=注册表键           <- 可选：注册表检测（HKLM\... / HKCU\...）
;    FileKey1=路径|模式|标志   <- 要清理的文件
;        路径   : 目录（支持 %LocalAppData% 等变量与通配符）
;        模式   : 分号分隔的通配符，如 *.tmp;*.cache（空=整个目录）
;        标志   : RECURSE 表示递归子目录
;    Warning=                   <- 若存在，则该规则标记为「需谨慎」
;    Default=说明文字           <- 规则描述（详情面板显示）
;
;  多个 FileKey（FileKey1/FileKey2...）会生成多个子项。
;  注意：仅匹配「模式」的文件会被删除，目录内其他文件不受影响。
; ============================================================

[My Tool Cache]
DetectFile=%LocalAppData%\MyTool
FileKey1=%LocalAppData%\MyTool\Cache|*.tmp;*.cache|RECURSE
Default=示例：清理 MyTool 的临时与缓存文件。改完文件名前缀即可启用。
